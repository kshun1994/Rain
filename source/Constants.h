#pragma once

// Game logic tick duration (in milliseconds)
const float CONST_TICK_DURATION = 1000.f / 60.f;

const float CONST_METER_MAX = 100.f;

const int CONST_MAX_INPUT_BUFFER = 60;
const int CONST_BUFFER_236 = 15; // buffer window length for registering a 236 motion
